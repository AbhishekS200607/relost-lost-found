# Production configuration
spring.profiles.active=prod

# Database (Supabase PostgreSQL)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://db.lgaopjetubdqnpnjopwc.supabase.co:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:asn@2006}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# JWT
jwt.secret=${JWT_SECRET:your-256-bit-secret-key-here-make-it-very-long-and-secure}
jwt.expiration=86400000

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server
server.port=${PORT:8080}